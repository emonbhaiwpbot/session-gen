services:
  - type: web
    name: emon-bhai-pairing
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20.20.2
